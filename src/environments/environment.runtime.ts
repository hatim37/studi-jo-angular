export const environment = {
  production: true,
  backend_products: (window as any).NG_APP_BACKEND_PRODUCTS,
  backend_user: (window as any).NG_APP_BACKEND_USER,
  backend_validation: (window as any).NG_APP_BACKEND_VALIDATION,
  backend_login: (window as any).NG_APP_BACKEND_LOGIN,
  backend_cart: (window as any).NG_APP_BACKEND_CART,
  backend_orders: (window as any).NG_APP_BACKEND_ORDERS,
};

